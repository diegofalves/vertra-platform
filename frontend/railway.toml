[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "PORT=3000 node .next/standalone/server.js"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
